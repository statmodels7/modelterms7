% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmented.R
\name{SegTerm}
\alias{SegTerm}
\title{S7 Class for Segmented and Stepmented Terms}
\usage{
SegTerm(
  label = character(0),
  X = NULL,
  coef_names = character(0),
  blueprint = list(),
  penalty = NULL,
  kind = character(0),
  var = NULL,
  npsi = integer(0),
  by = NULL,
  linear = logical(0),
  penalty_kind = character(0),
  spec = list()
)
}
\arguments{
\item{label}{A character string prefixed to the coefficient names when
non-empty.}

\item{X}{The design block, filled by \code{\link{term_build}}.}

\item{coef_names}{The coefficient names, filled by
\code{\link{term_build}}.}

\item{blueprint}{The information needed to reproduce the mapping on new
data, filled by \code{\link{term_build}}.}

\item{penalty}{The penalty on the term's coefficients, or \code{NULL}.}

\item{kind}{Which of the three constructions.}

\item{var}{The covariate expression.}

\item{npsi}{The number of break-points.}

\item{by}{An optional grouping expression.}

\item{linear}{Whether the block carries the linear effect.}

\item{penalty_kind}{The penalty on the changes, if any.}

\item{spec}{The resolved construction settings.}
}
\value{
An object of class \code{SegTerm}.
}
\description{
A subclass of \code{\link{additive_term}} for a covariate whose effect
changes at estimated break-points: a change of slope
(\code{\link{seg}}), a jump in level (\code{\link{jump}}), or both at
the same points (\code{\link{jseg}}). The design block is the working
one of the iteration that estimates the break-points, and is
recomputed by \code{\link{term_refresh}} as they move.
}
\examples{
S7::S7_inherits(seg(x), SegTerm)
}
\seealso{
\code{\link{seg}}
}
