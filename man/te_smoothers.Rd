% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth.R
\name{te_smoothers}
\alias{te_smoothers}
\title{One Smoother per Margin of a Tensor Product}
\usage{
te_smoothers(smooths, nv)
}
\arguments{
\item{smooths}{One smoother, or a list of one per covariate.}

\item{nv}{How many covariates.}
}
\value{
A list of \code{nv} smoothers.
}
\description{
Resolves the \code{smooths} argument of \code{\link[=te]{te()}} into one smoother per covariate,
and rejects a margin asking for what the product does not read from it.
}
\details{
The product reads a margin's basis and its roughness matrix, so \code{k},
\code{degree}, \code{order}, \code{measure} and the interval all act. The constraint, the
null space and the reparametrization are the \strong{product's}: the tensor
basis contains the constant whatever its margins do, so the block carries
the sum-to-zero constraint over the observed covariates rather than a
marginal one, and the marginal linear effects are not separated out as
\code{\link[=s]{s()}} separates its one.

Those three are therefore rejected at a non-default value rather than
accepted and ignored, which would report a fit of a model the caller did
not ask for.
}
\keyword{internal}
