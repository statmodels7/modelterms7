% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{term_group}
\alias{term_group}
\alias{term_group.model_term}
\title{The Grouping a Term's Coefficients Are Indexed By}
\usage{
term_group(term, ...)
}
\arguments{
\item{term}{A built term. An unbuilt one returns \code{NULL}.}

\item{...}{Passed to methods. No shipped method reads anything here.}
}
\value{
A list with \code{expr} (a language object), \code{levels} (a character
vector) and \code{dim} (a single integer), or \code{NULL}.
}
\description{
For a term whose coefficients are one set per level of a grouping variable,
the expression that variable came from, the levels it took, and how many
columns each level carries. \code{NULL} for every other term.
}
\details{
Two labelled terms belong to the same covariance block only if they share a
grouping, and deciding that needs both halves: the expression alone is not
enough, since \code{droplevels(id)} and \code{id} are different expressions for the
same grouping and \code{id} under two subsets is the same expression for two
different ones. The levels settle it, and the expression makes a message
readable.

\code{dim} is the number of columns one level carries, which with the levels
says how the block is laid out: the coefficients are group-major, so level
\eqn{i} occupies positions \eqn{(i-1)d + 1} to \eqn{id}. A consumer stacking
two such blocks into one covariance needs exactly that.

The base method returns \code{NULL}, which is the answer for every term but
\code{\link[=random]{random()}}.
}
\examples{
d <- data.frame(x = rnorm(6), g = factor(rep(c("a", "b", "c"), 2)))
b <- term_build(random(~ 1 + x | g), d)
g <- term_group(b)
c(dim = g$dim, levels = length(g$levels))

# every other kind of term has none
term_group(term_build(linpar(~ x), d))

}
\seealso{
\code{\link[=term_tag]{term_tag()}} for the label that, with this, identifies a
covariance block; \code{\link[=random]{random()}} for the formula both are read from.
}
