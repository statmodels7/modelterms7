% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmented.R
\name{term_predict.SegTerm}
\alias{term_predict.SegTerm}
\title{A Break-Point Term's Block at New Rows}
\arguments{
\item{term}{A built \code{\link[=SegTerm]{SegTerm()}}. An unbuilt one throws
\code{"the term has not been built; call term_build(term, data) first."}.}

\item{newdata}{A data frame carrying the covariate and whatever the
subformulas name.}

\item{...}{Unused.}
}
\value{
A block of \code{nrow(newdata)} rows and \code{\link[=term_npar]{term_npar()}} columns, with the
term's coefficient names as column names.
}
\description{
The working block evaluated at \code{newdata}, at the break-points and the
coefficients the term currently carries. The positions are not re-derived
from the new rows, and any development of a coefficient is reapplied through
its sub-terms' own blueprints.
}
\details{
Re-deriving the break-points would give a different model at every set of
rows, which is the general reason \code{\link[=term_predict]{term_predict()}} reapplies instead of
rebuilding. For a break-point term it is sharper than usual: the positions
are what the fit estimated, and a subset of the data may not even contain
one.

\code{\link[=term_value]{term_value()}} with \code{newdata} is what gives the contribution there. For
\code{seg} the two differ, the block being a Jacobian.
}
\examples{
set.seed(1)
d <- data.frame(x = sort(runif(120, 0, 10)))
d$y <- 1 + 0.5 * d$x + 2 * pmax(d$x - 6, 0) + rnorm(120, sd = 0.4)
b <- term_refresh(term_build(seg(x, npsi = 1), d), c(0.5, 2, 6))

# On the fitting data it is the block itself.
max(abs(term_predict(b, d) - term_matrix(b)))

# At other rows the break-point is the fitted one, not one re-derived
# from those rows: here every row is below it.
nd <- data.frame(x = c(1, 2, 3))
term_predict(b, nd)
seg_psi(b)

}
\seealso{
\code{\link[=term_predict]{term_predict()}} for the generic, \code{\link[=term_value]{term_value()}} for the
contribution, \code{\link[=seg_psi]{seg_psi()}} for the positions it is evaluated at.
}
\keyword{internal}
