% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper.R
\name{reject_pathless_values}
\alias{reject_pathless_values}
\title{Reject a Written-Out Grid Where There Is No Path}
\usage{
reject_pathless_values(values, pen, what = "this term")
}
\arguments{
\item{values}{The written-out grids, as \code{\link[=check_values]{check_values()}} returns
them.}

\item{pen}{The penalty the term will build, or \code{NULL}.}

\item{what}{The term's label, for the message.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Signals an error when the caller gave several values for a hyperparameter
whose penalty has no kink.
}
\details{
Several values are a grid for a path to visit, and only a penalty with a
kink is swept along one: everything else has its hyperparameter read at the
mode by a marginal criterion, which would take the vector and do nothing
with it. The question is put to the penalty at a probe value of its own
hyperparameters, the kink set being structural, so a ridge and a random
effect under a Gaussian prior are covered by the same line as a family
added later.
}
\seealso{
\code{\link[=check_values]{check_values()}}, \code{\link[=term_values]{term_values()}}
}
\keyword{internal}
