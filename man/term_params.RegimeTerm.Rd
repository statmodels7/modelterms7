% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{term_params.RegimeTerm}
\alias{term_params.RegimeTerm}
\title{The Parameters of a Regime Term}
\arguments{
\item{term}{A \code{\link[=RegimeTerm]{RegimeTerm()}}, built or not: the names depend on \code{k} alone.}

\item{...}{Unused.}
}
\value{
A character vector of length \eqn{K + K(K-1)}.
}
\description{
\code{"level1"}, then \code{"gap2"} ... \code{"gapK"}, then the free names of the
transition matrix, \code{"alr1.1"} and the rest. A chain of \eqn{K} regimes has
\eqn{K + K(K-1)} parameters in all: one free level, \eqn{K-1} positive gaps
that order the others, and \eqn{K(K-1)} additive log-ratios, \eqn{K-1} per
row of the matrix.
}
\details{
The order matters: it is the order \code{\link[=term_links]{term_links()}}, \code{\link[=term_start]{term_start()}},
\code{\link[=term_loglik]{term_loglik()}}'s Jacobian columns and the joint variance matrix are all
indexed by, and \code{psi} must supply the names whatever order they come in.

The log-ratio names come from \code{\link[parameters7:transition_matrix]{parameters7::transition_matrix()}}'s own
\code{free_names}, so the spelling is that package's.
}
\examples{
term_params(regime(2))
term_params(regime(3))
vapply(2:4, function(k) length(term_params(regime(k))), integer(1))

}
\seealso{
\code{\link[=term_links]{term_links()}} for the chart each rides, \code{\link[=regime]{regime()}} for what they
mean.
}
\keyword{internal}
