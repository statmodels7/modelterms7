% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper.R
\name{term_search}
\alias{term_search}
\alias{term_search.model_term}
\title{How a Term Covers Its Own Kinked Hyperparameters}
\usage{
term_search(term, ...)
}
\arguments{
\item{term}{A term, built or not.}

\item{...}{Passed to methods. No shipped method reads anything here.}
}
\value{
A named list, one entry per penalty of the term, each the single
string \code{"grid"} or \code{"cyclic"}. Empty where the term names none.
}
\description{
Reports whether the term's own hyperparameters are swept as a product,
\code{"grid"}, or one at a time with the others held, \code{"cyclic"}. A term that
names neither is covered the way the fitting layer covers one by default.
}
\details{
It matters only for a term carrying \strong{more than one} hyperparameter with a
kink, so \code{\link[=enet]{enet()}}, \code{\link[=scad]{scad()}} and \code{\link[=mcp]{mcp()}}; there is nothing to combine
otherwise, and \code{\link[=lasso]{lasso()}} does not take the argument. Under \code{"grid"} the cost
is the product of the term's own grids, 25 by 5 at the defaults; under
\code{"cyclic"} it is their sum per pass.

It is one word per penalty. It says how the hyperparameters are combined
\strong{with each other}, which is not a property any one of them has.

Between two terms the sweep alternates whatever each one names, so
\code{y ~ lasso(X) + enet(R)} costs the two blocks added, so one term asking for
a product does not make the other pay for it.

The keys are \code{\link[=term_penalties]{term_penalties()}}'s entry names, \code{""} for a penalty over the
whole term.
}
\examples{
# The default is a product of the two axes; cyclic sweeps one at a time.
term_search(enet(~ x))
term_search(enet(~ x, search = "cyclic"))

# One hyperparameter, so nothing to combine and no argument to take.
term_search(lasso(~ x))
try(lasso(~ x, search = "cyclic"))

}
\seealso{
\code{\link[=term_grid]{term_grid()}} for the sizes being combined, \code{\link[=term_path_min]{term_path_min()}} for
the depth of the kink-size axis.
}
