% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal.R
\name{term_params.MarginalBreakTerm}
\alias{term_params.MarginalBreakTerm}
\title{The Parameters of a Marginal Break-Point Term}
\arguments{
\item{term}{A \code{\link[=MarginalBreakTerm]{MarginalBreakTerm()}}, built or not.}

\item{...}{Unused.}
}
\value{
A character vector, of length \code{\link[=term_npar]{term_npar()}}.
}
\description{
Numbered, one set per break-point, in a fixed order: the linear effect
\code{beta} where the kind carries one, then the prior's parameters for each
break-point, then the changes of slope \code{gamma1} ... and the changes of
level \code{delta1} ....
}
\details{
The prior's parameters are \code{mk} and \code{tauk} under the default Gaussian, the
location and the scale of break-point \eqn{k}. Where \code{random(distrib = )}
named another family the names are that family's own, its location fixed at
zero and \code{mk} carrying the position, so a Student t prior adds \code{nuk}.

Which of \code{gamma} and \code{delta} appear is the kind: \code{"seg"} has the changes of
slope, \code{"jump"} the changes of level, \code{"jseg"} both. Only \code{"seg"} and
\code{"jseg"} carry \code{beta}.
}
\examples{
set.seed(1)
dd <- data.frame(id = rep(1:3, each = 8), x = rep(1:8, 3))
dd$y <- rnorm(24, 2 * (dd$x >= 4.5), 0.4)

# A step term: the prior's location and scale, and the change of level.
term_params(term_build(jump(x, psi ~ random(~ 1 | id), marginal = TRUE), dd))

# A continuous one adds the linear effect and the change of slope.
term_params(term_build(seg(x, psi ~ random(~ 1 | id), marginal = TRUE), dd))

}
\seealso{
\code{\link[=term_links]{term_links()}} for the chart each rides, \code{\link[=MarginalBreakTerm]{MarginalBreakTerm()}} for
what they mean.
}
\keyword{internal}
