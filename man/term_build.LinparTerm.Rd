% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linpar.R
\name{term_build.LinparTerm}
\alias{term_build.LinparTerm}
\title{Build a Parametric Block}
\arguments{
\item{term}{An unbuilt or built \code{\link[=LinparTerm]{LinparTerm()}}.}

\item{data}{A data frame carrying every variable the formula names.}

\item{...}{Unused.}
}
\value{
The term with \code{X}, \code{coef_names} and \code{blueprint} filled, so that
\code{\link[=term_is_built]{term_is_built()}} is \code{TRUE}. The block has \code{nrow(data)} rows, and its
column names are the coefficient names.
}
\description{
Builds the model matrix of a \code{\link[=linpar]{linpar()}} term's formula against \code{data},
prefixes the coefficient names with the term's label, and records the
blueprint \code{\link[=term_predict]{term_predict()}} will reapply: the terms object with the response
deleted, the factor levels, the contrasts used, and the storage settled on.
}
\section{The model frame}{
\code{\link[stats:model.frame]{stats::model.frame()}} is called with \code{na.action = na.pass}, so a row with a
missing covariate keeps its place and the block stays aligned with the
response, and with \code{drop.unused.levels = FALSE}, so a factor level present
in the data but used by no row still gets a column. Both choices are about
alignment: a block whose rows have been silently dropped no longer matches
the response it is fitted against.
}

\section{The storage}{
Where the constructor left \code{sparse = NULL}, \code{\link[=.resolve_sparse]{.resolve_sparse()}} settles it
from \code{n} times the indicator column count. The value settled on goes into
\code{blueprint$sparse} and the \code{sparse} property is left as the caller wrote it,
because new rows may be any number and a prediction deciding again could
build a block of a different kind from the fitted one.
}

\examples{
dd <- data.frame(x = 1:8, g = factor(rep(c("a", "b", "c", "d"), 2)))
b <- term_build(linpar(~ x + g), dd)
term_matrix(b)
names(b@blueprint)

# A missing value keeps its row.
term_matrix(term_build(linpar(~ x), data.frame(x = c(1, NA, 3))))

}
\seealso{
\code{\link[=linpar]{linpar()}}, \code{\link[=term_predict.LinparTerm]{term_predict.LinparTerm()}}, \code{\link[=.resolve_sparse]{.resolve_sparse()}}.
}
\keyword{internal}
