% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random.R
\name{term_group.RandomTerm}
\alias{term_group.RandomTerm}
\title{The Grouping of a Random-Effect Term}
\arguments{
\item{term}{A built \code{\link[=RandomTerm]{RandomTerm()}}.}

\item{...}{Unused.}
}
\value{
A list with \code{expr}, \code{levels} and \code{dim}, or \code{NULL}.
}
\description{
The grouping expression, its levels and the number of within-group columns,
or \code{NULL} on an unbuilt term.
}
\details{
The three come from the blueprint, where the build recorded them. \code{dim} is
the within-group column count, so the block's level \eqn{i} occupies
positions \eqn{(i-1)d + 1} to \eqn{id}, which is the group-major order the
blockwise penalty reads.
}
\seealso{
\code{\link[=term_group]{term_group()}} for the generic.
}
\keyword{internal}
