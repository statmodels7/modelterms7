% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper.R
\name{smooth_hyper}
\alias{smooth_hyper}
\title{The Held Smoothing Parameters of a Smooth}
\usage{
smooth_hyper(lambda, names, what = "this smooth")
}
\arguments{
\item{lambda}{What the constructor was given, or \code{NULL}.}

\item{names}{The penalty's own hyperparameter names.}

\item{what}{The term's label, for the message.}
}
\value{
A named list of held values.
}
\description{
Carries the \code{lambda} a smooth's constructor was given onto the names
its penalty uses, and checks it against the positivity every smoothing
parameter obeys.
}
\details{
A one-dimensional smooth and an isotropic tensor product carry one
smoothing parameter, named \code{lambda}; an anisotropic tensor product
carries one per margin, \code{lambda1}, \code{lambda2} and so on, which is
what \code{\link[penalties7:additive_penalty]{penalties7::additive_penalty()}} names them. An unnamed
vector is read in that order and must be as long as there are margins; a
named one may hold some and leave the others to be estimated.
}
\seealso{
\code{\link[=check_hyper]{check_hyper()}}, \code{\link[=s]{s()}}, \code{\link[=te]{te()}}
}
\keyword{internal}
