% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal.R
\name{print.MarginalBreakTerm}
\alias{print.MarginalBreakTerm}
\title{Print a Marginal Break-Point Term}
\arguments{
\item{x}{A \code{\link[=MarginalBreakTerm]{MarginalBreakTerm()}}, built or not.}

\item{...}{Unused, and accepted so that the signature matches \code{\link[=print]{print()}}'s.}
}
\value{
\code{x}, invisibly. Called for the lines it writes.
}
\description{
Prints the label and the kind, how many latent break-points each group
carries, and, for a built term, over how many groups. A second line lists
the parameters, and a third names the prior where one was given.
}
\details{
The form is

\if{html}{\out{<div class="sourceCode">}}\preformatted{<MarginalBreakTerm> 'jump' (jump): 1 latent break-point per group,
                    integrated out (3 groups)
  parameters: m1, tau1, delta1
}\if{html}{\out{</div>}}

The prior line appears only where \code{random(distrib = )} named a family; under
the default Gaussian there is nothing to name. A built structural term is
never described as "built", the group count being the tell:
\code{\link[=term_is_built]{term_is_built()}} tests for a design block, which this branch does not have.
}
\examples{
set.seed(1)
dd <- data.frame(id = rep(1:3, each = 8), x = rep(1:8, 3))
dd$y <- rnorm(24, 2 * (dd$x >= 4.5), 0.4)

# A specification, and the same term built over three groups.
jump(x, psi ~ random(~ 1 | id), marginal = TRUE)
term_build(jump(x, psi ~ random(~ 1 | id), marginal = TRUE), dd)

}
\seealso{
\code{\link[=jump]{jump()}}, \code{\link[=term_params]{term_params()}}.
}
\keyword{internal}
