% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonlinear.R
\name{NlTerm}
\alias{NlTerm}
\title{S7 Class for Nonlinear Parametric Terms}
\usage{
NlTerm(
  label = character(0),
  hyper = list(),
  X = NULL,
  coef_names = character(0),
  blueprint = list(),
  penalty = NULL,
  fn = NULL,
  nl_params = character(0),
  links = list(),
  subformulas = list(),
  deriv_mode = character(0),
  spec = list()
)
}
\arguments{
\item{label}{A character string prefixed to the coefficient names when
non-empty.}

\item{hyper}{The hyperparameters of the term's penalty that the
caller HELD, as a named list. Empty, the default, means every one
of them is estimated. See \code{\link{term_hyper}}.}

\item{X}{The design block, filled by \code{\link{term_build}}.}

\item{coef_names}{The coefficient names, filled by
\code{\link{term_build}}.}

\item{blueprint}{The information needed to reproduce the mapping on new
data, filled by \code{\link{term_build}}.}

\item{penalty}{The penalty on the term's coefficients, or \code{NULL}.}

\item{fn}{The function or formula defining the contribution.}

\item{nl_params}{The names of the nonlinear parameters.}

\item{links}{One link per parameter.}

\item{subformulas}{One optional formula per parameter.}

\item{deriv_mode}{How the derivatives are obtained.}

\item{spec}{The resolved construction settings.}
}
\value{
An object of class \code{NlTerm}.
}
\description{
A subclass of \code{\link{additive_term}} for a parametric function
that is nonlinear in its own parameters. The design block is the
Jacobian of that function in the parameters, so the term is linear in
the sense the model layer needs while the function is not; the block
depends on where the parameters currently are and is recomputed by
\code{\link{term_refresh}}.
}
\examples{
S7::S7_inherits(nl(~ theta1 * exp(theta2 * x)), NlTerm)
}
\seealso{
\code{\link{nl}}
}
