% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalized.R
\name{term_build.PenalizedTerm}
\alias{term_build.PenalizedTerm}
\title{Build a Penalized Block and Attach Its Penalty}
\arguments{
\item{term}{An unbuilt or built \code{\link[=PenalizedTerm]{PenalizedTerm()}}.}

\item{data}{A data frame with as many rows as a matrix input has, carrying
every variable a formula input names.}

\item{...}{Unused.}
}
\value{
The term with \code{X}, \code{coef_names}, \code{blueprint} and \code{penalty} filled.
\code{penalty@n_coef} equals \code{ncol(X)}.
}
\description{
Builds the block of a \code{\link[=ridge]{ridge()}}, \code{\link[=lasso]{lasso()}}, \code{\link[=enet]{enet()}}, \code{\link[=scad]{scad()}} or \code{\link[=mcp]{mcp()}}
term, names its coefficients, records the blueprint, and calls the term's
factory to produce the \pkg{penalties7} penalty over exactly that many
coefficients. The penalty exists only after this: a specification's
\code{penalty} property is \code{NULL}, its width being unknown until the data are
seen.
}
\section{Two input routes, two blueprints}{
A \strong{formula} is built through \code{\link[stats:model.frame]{stats::model.frame()}} and
\code{\link[=.design_matrix]{.design_matrix()}} with the intercept removed by
\code{update(formula, ~ . - 1)}, unless the intercept is all the formula has, in
which case it is kept and is the block. The blueprint records
\code{kind = "formula"}, the terms object, the levels, the contrasts and the
settled storage.

A \strong{matrix} is used as it stands, and its row count must equal
\code{nrow(data)}; anything else throws with both numbers. Its columns take the
matrix's own names, or \code{1}, \code{2}, ... where it has none. The blueprint
records \code{kind = "matrix"}, the expression that produced it and those base
names.

Either way every column name is prefixed with the term's label, and the
block's row names are dropped.
}

\section{Standardization is applied to the penalty}{
With \code{standardize = TRUE} the column spreads come from \code{\link[=.block_sd]{.block_sd()}}, go
into \code{blueprint$standardize} and become a \code{\link[Matrix:Diagonal]{Matrix::Diagonal()}} map. The
design is not touched, so a sparse block stays sparse.

The map is passed to the factory \strong{as a second argument, and only when there
is one}. A factory written before standardization existed takes the count
alone and goes on working, provided it is never standardized.
}

\examples{
set.seed(3)
dd <- data.frame(x1 = rnorm(20), x2 = rnorm(20))

# The penalty appears at build, over exactly the columns built.
b <- term_build(lasso(~ x1 + x2), dd)
c(cols = ncol(term_matrix(b)), n_coef = b@penalty@n_coef)

# A matrix input must have as many rows as the data.
M <- matrix(rnorm(30), 10, 3)
try(term_build(ridge(M), dd))

}
\seealso{
\code{\link[=penalized_terms]{penalized_terms()}} for what the five constructors share,
\code{\link[=term_predict.PenalizedTerm]{term_predict.PenalizedTerm()}} for the block at new rows,
\code{\link[=.block_sd]{.block_sd()}} for the spreads.
}
\keyword{internal}
