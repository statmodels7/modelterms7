% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{term_tag}
\alias{term_tag}
\alias{term_tag.model_term}
\title{The Covariance Label of a Term}
\usage{
term_tag(term, ...)
}
\arguments{
\item{term}{A term, built or not.}

\item{...}{Passed to methods. No shipped method reads anything here.}
}
\value{
A single string, or \code{NA_character_} where the term carries no label.
}
\description{
The label a term carries to say that its coefficients share a covariance
block with those of other terms, or \code{NA_character_} where it carries none.
}
\details{
The label is written in the middle position of a bar formula,
\code{random(~ 1 + x | u | id)}, following \pkg{brms}: the last position is the
grouping variable and the middle one the label. Terms carrying the same
label and the same grouping describe effects that are correlated with each
other, and a fitting layer collects them into one block.

The label is a \strong{name and not data}. It is read as a symbol and never
evaluated, so a column of the data with the same name is not looked at.

The base method returns \code{NA_character_}, which is the answer for every term
but \code{\link[=random]{random()}}, so a term written outside this package needs no method here.
}
\examples{
# No label: the effects of this term correlate with nothing outside it.
term_tag(random(~ 1 | g))
term_tag(linpar(~ x))

# A label, read from the middle of the two bars.
term_tag(random(~ 1 + x | u | g))

# It is a name, not data: a column called `u` is not read.
d <- data.frame(x = rnorm(6), u = 1:6, g = factor(rep(c("a", "b"), 3)))
identical(term_coef_names(term_build(random(~ 1 | u | g), d)),
          term_coef_names(term_build(random(~ 1 | g), d)))

}
\seealso{
\code{\link[=random]{random()}}, the one constructor that reads a label;
\code{\link[=term_penalties]{term_penalties()}} for the penalties a term declares on its own.
}
