% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random.R
\name{term_build.RandomTerm}
\alias{term_build.RandomTerm}
\title{Build a Random-Effect Term}
\arguments{
\item{term}{An unbuilt or built \code{\link[=RandomTerm]{RandomTerm()}}.}

\item{data}{A data frame carrying the grouping variable and the
within-group covariates.}

\item{...}{Unused.}
}
\value{
The term with \code{X} (a \code{dgCMatrix} of \eqn{md} columns),
\code{coef_names}, \code{blueprint} and \code{penalty} filled.
}
\description{
Builds the within-group design from the left of the bar, interacts it with
the group indicators, and attaches the effects' distribution as the penalty
on the resulting coefficients. The levels of the grouping variable are
recorded, so \code{\link[=term_predict]{term_predict()}} maps new rows onto the same ones.
}
\section{The block}{
With \eqn{m} levels and a within-group design \eqn{Z_i} of \eqn{d} columns,
the block is \eqn{\mathrm{diag}(Z_1, \dots, Z_m)}, ordered \strong{group-major},
so the \eqn{d} coefficients of one group are adjacent. It is built as a
\code{dgCMatrix}: a row belongs to one group, so the density is \eqn{1/m}.

The coefficient names are \code{label.level.column}, so \code{random(~ x | g)} over
three levels gives \code{random.a.(Intercept)}, \code{random.a.x},
\code{random.b.(Intercept)} and so on, which is the group-major order read off.
}

\section{The penalty, and what the build checks}{
Where \code{distrib} is \code{NULL} the default is chosen here: a centered
\code{gaussian1_distrib} at one column or under \code{correlated = FALSE}, and a
centered multivariate Gaussian on an unstructured covariance for several
correlated ones.

Whatever the distribution, its location parameters must be \strong{held}. A free
location is confounded with the intercept of the equation the term sits in,
and the build rejects it with a message naming the parameter and the fix. A
multivariate distribution must also match the within-group dimension, and
one that carries no matrix parameter cannot express correlated effects at
all.

Any value named in \code{hyper} is checked here too, against the penalty's own
names, this being the first point at which the penalty exists.
}

\examples{
set.seed(1)
dd <- data.frame(x = rnorm(9), g = factor(rep(c("a", "b", "c"), 3)))

# Group-major: the two coefficients of one level are adjacent.
b <- term_build(random(~ x | g), dd)
term_coef_names(b)
as.matrix(term_matrix(b))

# A free location is refused, naming the parameter.
try(term_build(random(~ 1 | g,
                      distrib = distributions7::gaussian1_distrib()), dd))

}
\seealso{
\code{\link[=random]{random()}}, \code{\link[=term_predict.RandomTerm]{term_predict.RandomTerm()}}, \code{\link[=term_penalties]{term_penalties()}}.
}
\keyword{internal}
