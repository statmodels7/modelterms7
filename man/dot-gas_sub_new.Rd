% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gas_submodels.R
\name{.gas_sub_new}
\alias{.gas_sub_new}
\title{The Filter's Parameters at Rows Outside the Fitting Data}
\usage{
.gas_sub_new(term, u, newdata)
}
\arguments{
\item{term}{A built \code{GasTerm} whose blueprint carries at least one
subformula. With no development the caller uses \code{.gas_coefs()} instead.}

\item{u}{The term's parameter vector, in \code{\link[=term_params]{term_params()}} order, on the mixed
scale above.}

\item{newdata}{A data frame of the rows to read at, carrying every variable
the subformulas name.}
}
\value{
A list of three:
\describe{
\item{\code{om}}{numeric, one \eqn{\omega_t} per row of \code{newdata}.}
\item{\code{A}}{\code{nrow(newdata)} by \code{p}, the score loadings.}
\item{\code{B}}{\code{nrow(newdata)} by \code{q}, the autoregressive coefficients,
Levinson-Durbin applied row by row. A zero-column matrix at
\eqn{q = 0}.}
}
}
\description{
Evaluates the recursion's coefficients \eqn{(\omega_t, \alpha_t, \beta_t)}
at each row of \code{newdata}, so that \code{\link[=term_continue]{term_continue()}} can carry a score-driven
filter past the end of its series. A parameter carrying a subformula is read
by calling \code{\link[=term_predict]{term_predict()}} on each of its sub-terms, which reapplies the
blueprint each one recorded at build time; a basis or a set of contrasts is
therefore not relearned on the new rows. A parameter with no subformula is
constant along them.
}
\details{
For a developed parameter \eqn{j} the value at row \eqn{t} is
\eqn{\psi_{j,t} = g_j^{-1}(z_t' \gamma_j)} with \eqn{g_j} the parameter's own
link and \eqn{\gamma_j} its coefficients, and the persistence goes on to
Levinson-Durbin, so \code{B} holds autoregressive coefficients where \code{u} holds
partial autocorrelations.

\code{u} mixes two scales, matching what \code{\link[=term_filter]{term_filter()}} is handed: a parameter
with no subformula is taken as it stands, on the parameter scale, while a
developed parameter's entries are its coefficients on the unconstrained
scale and go through the chart here. With \eqn{p = q = 1} and no
development, \code{u = c(0.3, 0.4, 0.7)} gives \eqn{\omega = 0.3},
\eqn{\alpha_1 = 0.4} and a partial autocorrelation of 0.7.
}
\seealso{
\code{\link[=term_continue]{term_continue()}}, the only caller; \code{\link[=term_filter]{term_filter()}} for the same
quantities inside the series.
}
\keyword{internal}
