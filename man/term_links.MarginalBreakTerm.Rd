% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal.R
\name{term_links.MarginalBreakTerm}
\alias{term_links.MarginalBreakTerm}
\title{The Charts of a Marginal Break-Point Term's Parameters}
\arguments{
\item{term}{A \code{\link[=MarginalBreakTerm]{MarginalBreakTerm()}}, built or not.}

\item{...}{Unused.}
}
\value{
A named list of \pkg{linkfunctions7} links, one per entry of
\code{\link[=term_params]{term_params()}}.
}
\description{
The \strong{log} link on every \code{tauk}, a prior's own link on any parameter it
contributes, and the identity on everything else. A prior scale must be
positive; a position, a change of level and a change of slope are already
unconstrained.
}
\details{
Where \code{random(distrib = )} named a family other than the Gaussian, that
family's parameters keep the links it declares in \code{link_params}, so a
Student t prior's degrees of freedom ride whatever chart
\pkg{distributions7} gives them. Nothing about the chart is restated here.
}
\examples{
set.seed(1)
dd <- data.frame(id = rep(1:3, each = 8), x = rep(1:8, 3))
dd$y <- rnorm(24, 2 * (dd$x >= 4.5), 0.4)
tm <- term_build(jump(x, psi ~ random(~ 1 | id), marginal = TRUE), dd)
vapply(term_links(tm), function(l) l@link_name, character(1))

}
\seealso{
\code{\link[=term_params]{term_params()}}, \code{\link[=term_start]{term_start()}}.
}
\keyword{internal}
