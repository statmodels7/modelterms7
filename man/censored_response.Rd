% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response.R
\name{censored_response}
\alias{censored_response}
\title{S7 Class for a Censored Response}
\usage{
censored_response(
  y = integer(0),
  lwr = integer(0),
  upr = integer(0),
  status = character(0)
)
}
\arguments{
\item{y}{The numeric response values (\code{NA} for an
interval-censored observation).}

\item{lwr, upr}{The numeric censoring bounds, one value per observation.}

\item{status}{The character vector of per-observation statuses.}
}
\value{
An object of class \code{censored_response}.
}
\description{
The response object \code{\link[=cens]{cens()}} constructs: the observed values,
the per-observation censoring bounds, and the status each observation
carries (\code{"observed"}, \code{"left"}, \code{"right"} or
\code{"interval"}). The likelihood assembler of the model layer consumes
it, contributing a density where the observation is exact and a
difference of distribution functions where it is censored.
}
\examples{
S7::S7_inherits(cens(c(0, 1.2), lwr = 0), censored_response)
}
\seealso{
\code{\link[=cens]{cens()}}
}
