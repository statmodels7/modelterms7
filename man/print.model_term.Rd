% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{print.model_term}
\alias{print.model_term}
\title{Print a Model Term}
\arguments{
\item{x}{A term, built or not.}

\item{...}{Unused, and accepted so that the signature matches \code{\link[=print]{print()}}'s.}
}
\value{
\code{x}, invisibly. Called for the line it writes.
}
\description{
Prints one line naming the term's class, its label when it has one, and
whether it is built. A built term reports how many coefficients its block
carries; a specification says so and names the call that would build it.
This is the default for every term class, and several classes override it to
add what they alone carry.
}
\details{
The two forms are

\if{html}{\out{<div class="sourceCode">}}\preformatted{<SmoothTerm> 's(x)' built: 4 coefficients
<SmoothTerm> 's(x)' (specification; call term_build() with data)
}\if{html}{\out{</div>}}

The class name is the S7 class's own, the label comes from the \code{label}
property and is omitted when empty, and the count is \code{ncol(x@X)}.

The classes that override this add something of their own:
\code{\link[=print.PenalizedTerm]{print.PenalizedTerm()}} names the penalty and the standardization,
\code{\link[=gas]{gas()}}'s and \code{\link[=regime]{regime()}}'s report their parameters, \code{\link[=nl]{nl()}}'s its formula and
\code{\link[=seg]{seg()}}'s its break-points. A structural term is never reported as built
here, \code{\link[=term_is_built]{term_is_built()}} testing for a design block.
}
\examples{
d <- data.frame(x = 1:4)

# A specification, and the same term built.
linpar(~ x)
term_build(linpar(~ x), d)

# The label is shown when there is one.
s(x, k = 5)
linpar(~ x, label = "lin")

}
\seealso{
\code{\link[=term_is_built]{term_is_built()}} for the predicate it branches on,
\code{\link[=term_coef_names]{term_coef_names()}} for the names behind the count.
}
\keyword{internal}
