% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random.R
\name{RandomTerm}
\alias{RandomTerm}
\title{S7 Class for Grouped Random-Effect Terms}
\usage{
RandomTerm(
  label = character(0),
  hyper = list(),
  X = NULL,
  coef_names = character(0),
  blueprint = list(),
  penalty = NULL,
  formula = NULL,
  correlated = logical(0),
  precision = NULL,
  distrib = NULL,
  kinks = integer(0)
)
}
\arguments{
\item{label}{A character string prefixed to the coefficient names when
non-empty.}

\item{hyper}{The hyperparameters of the term's penalty that the
caller HELD, as a named list. Empty, the default, means every one
of them is estimated. See \code{\link{term_hyper}}.}

\item{X}{The design block, filled by \code{\link{term_build}}.}

\item{coef_names}{The coefficient names, filled by
\code{\link{term_build}}.}

\item{blueprint}{The information needed to reproduce the mapping on new
data, filled by \code{\link{term_build}}.}

\item{penalty}{The penalty on the term's coefficients, or \code{NULL}.}

\item{formula}{The bar formula, e.g. \code{~ 1 | g} or \code{~ x | g}.}

\item{correlated}{Logical; whether the default Gaussian lets the
within-group effects correlate.}

\item{precision}{A \pkg{parameters7} matrix parameter for the
per-group precision, or \code{NULL}.}

\item{distrib}{A univariate \pkg{distributions7} object for the
effects, or \code{NULL}.}

\item{kinks}{The declared kink set of \code{distrib}'s log-density.}
}
\value{
An object of class \code{RandomTerm}.
}
\description{
A subclass of \code{\link{additive_term}} for grouped coefficients with
a distribution on the effects: the within-group design interacted with
the grouping indicators, one coefficient per group and per within-group
column, with the penalty carrying the effects' distribution.
Constructed by \code{\link{random}}.
}
\examples{
S7::S7_inherits(random(~ 1 | g), RandomTerm)
}
\seealso{
\code{\link{random}}
}
