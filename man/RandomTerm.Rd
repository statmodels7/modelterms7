% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random.R
\name{RandomTerm}
\alias{RandomTerm}
\title{S7 Class for Grouped Random-Effect Terms}
\usage{
RandomTerm(
  label = character(0),
  hyper = list(),
  grid = list(),
  values = list(),
  min_ratio = numeric(0),
  search = character(0),
  ids = character(0),
  X = NULL,
  coef_names = character(0),
  blueprint = list(),
  penalty = NULL,
  formula = NULL,
  correlated = logical(0),
  distrib = NULL,
  tag = character(0)
)
}
\arguments{
\item{label}{A character string prefixed to the term's coefficient names
when non-empty, and used as the title of \code{\link[=plot]{plot()}} and the tag of
\code{\link[=print]{print()}}. \code{character(0)} and \code{""} both mean no label.}

\item{hyper}{The hyperparameters of the term's penalty the caller \strong{held},
as a named list keyed by the penalty's names. Empty, the default, means
every one of them is estimated. See \code{\link[=term_hyper]{term_hyper()}}.}

\item{grid}{How many values a path visits for each of the term's
hyperparameters, as a named list of single whole numbers. Empty, the
default, leaves the number to the fitting layer. See \code{\link[=term_grid]{term_grid()}}.}

\item{values}{The values a path visits, for each hyperparameter the caller
wrote out, as a named list of numeric vectors. Empty, the default, has the
path build them. See \code{\link[=term_values]{term_values()}}.}

\item{min_ratio}{How far down the path over the size of the kink reaches, as
a fraction of the value that empties the block: one number in \eqn{(0, 1)},
or \code{numeric(0)} for the fitting layer's own. See \code{\link[=term_path_min]{term_path_min()}}.}

\item{search}{How the term's own hyperparameters are covered when it has
more than one carrying a kink: \code{"grid"} for every combination of them,
\code{"cyclic"} for one at a time, or \code{character(0)} for the default. See
\code{\link[=term_search]{term_search()}}.}

\item{ids}{Which of the term's hyperparameters are shared with those of
other terms, and under what label: a character vector named by the
term's own hyperparameters, or \code{character(0)} for none. The terms
carrying the same label for the same hyperparameter estimate one value.
See \code{\link[=term_ids]{term_ids()}}.}

\item{X}{The design block, one row per observation: a numeric matrix or a
two-dimensional \pkg{Matrix}. Empty until \code{\link[=term_build]{term_build()}} fills it.}

\item{coef_names}{The block's coefficient names, one per column, prefixed by
\code{label} when there is one. Filled by \code{\link[=term_build]{term_build()}}.}

\item{blueprint}{A named list of everything needed to reproduce the mapping
on new rows. Its contents are the subclass's business; nothing outside the
term reads them. Empty until \code{\link[=term_build]{term_build()}} fills it.}

\item{penalty}{A \pkg{penalties7} penalty on the block's coefficients, or
\code{NULL} when the term is unpenalized.}

\item{formula}{The bar formula, \code{~ 1 | g} or \code{~ x | g}, with the
within-group design on the left and the grouping variable on the right.}

\item{correlated}{A single logical: whether the default Gaussian lets the
within-group effects correlate. Read only when \code{distrib} is \code{NULL}.}

\item{distrib}{The effects' distribution, a \pkg{distributions7} object or
a list of them with one per within-group column, or \code{NULL} for the
default Gaussian.}

\item{tag}{The covariance label from the middle of two bars, or
\code{NA_character_}.}
}
\value{
An S7 object of class \code{RandomTerm}, inheriting from
\code{\link[=additive_term]{additive_term()}} and \code{\link[=model_term]{model_term()}}, with the four properties above
beside the ten they supply.
}
\description{
The subclass of \code{\link[=additive_term]{additive_term()}} holding grouped coefficients with a
distribution attached to them: the within-group design interacted with the
group indicators, one coefficient per group and per within-group column,
and a penalty carrying that distribution's negative log-density. \code{\link[=random]{random()}}
constructs it.
}
\section{The three properties of its own}{
\code{formula} is the bar formula as given, \code{~ 1 | g} or \code{~ x | g}, kept with its
environment. \code{correlated} says whether the \strong{default} Gaussian lets the
within-group effects depend on each other; it is read only where \code{distrib}
is \code{NULL}, the two saying the same thing.

\code{tag} is the covariance label written between two bars, \code{~ 1 | u | g}, or
\code{NA_character_} where the formula carries one bar. It is reported by
\code{\link[=term_tag]{term_tag()}}. The two say different things and the difference is the scope:
\code{correlated} is about the columns of this term, \code{tag} about the columns of
other terms.

\code{distrib} is the effects' distribution as supplied, or \code{NULL} for the
default. What the build turns it into is a \pkg{penalties7} penalty, read
through \code{\link[=term_penalty]{term_penalty()}} or \code{\link[=term_penalties]{term_penalties()}}, so the hyperparameter names
and their bounds all come from the distribution.
}

\section{The block is sparse by construction}{
A row belongs to one group, so the block has a density of \eqn{1/m} and is
always a \code{dgCMatrix}. \code{\link[=random]{random()}} takes no \code{sparse} argument for that reason,
and passing one is an error.
}

\examples{
set.seed(1)
dd <- data.frame(x = rnorm(9), g = factor(rep(c("a", "b", "c"), 3)))

tm <- random(~ x | g)
S7::S7_inherits(tm, RandomTerm)
tm@formula
tm@correlated

# The block is one diagonal block per group and is always sparse.
b <- term_build(tm, dd)
class(term_matrix(b))
term_coef_names(b)

# The hyperparameters are the effects' distribution's own.
term_penalty(b)@params

}
\seealso{
\code{\link[=random]{random()}}, the constructor; \code{\link[=term_penalties]{term_penalties()}} for the entries a
built one declares; \code{\link[=edf]{edf()}} for what a fitted random effect spends.
}
