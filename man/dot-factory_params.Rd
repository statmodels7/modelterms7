% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper.R
\name{.factory_params}
\alias{.factory_params}
\title{The Hyperparameter Names a Penalty Factory Produces}
\usage{
.factory_params(factory)
}
\arguments{
\item{factory}{A function of the coefficient count returning a penalty, or
a penalty.}
}
\value{
A character vector, empty where the penalty carries no
hyperparameter, or \code{NULL} where it could not be built.
}
\description{
Builds the penalty at a probe size and reads its \code{params}, returning an
empty vector where it cannot be built.
}
\details{
A factory is a function of the coefficient count, and the names it produces
do not depend on that count, so one coefficient is enough to read them. The
probe is wrapped because a factory may reject a size of one, and the two
outcomes are kept apart: \code{NULL} says nothing is known here, an empty vector
says the penalty carries no hyperparameter. Every shipped factory answers,
so \code{NULL} is reachable only from one a caller wrote.
}
\keyword{internal}
