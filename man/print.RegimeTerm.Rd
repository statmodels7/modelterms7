% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regime.R
\name{print.RegimeTerm}
\alias{print.RegimeTerm}
\title{Print a Regime Term}
\arguments{
\item{x}{A \code{\link[=RegimeTerm]{RegimeTerm()}}, built or not.}

\item{...}{Unused, and accepted so that the signature matches \code{\link[=print]{print()}}'s.}
}
\value{
\code{x}, invisibly. Called for the two lines it writes.
}
\description{
Prints two lines: the label and the number of regimes, followed by the
term's parameters. A built term reports how many groups the recursion runs
over; a specification says so instead.
}
\details{
The two forms are

\if{html}{\out{<div class="sourceCode">}}\preformatted{<RegimeTerm> 'regime': 2 regimes (specification)
  parameters: level1, gap2, alr1.1, alr2.1

<RegimeTerm> 'regime': 2 regimes; 1 group(s)
  parameters: level1, gap2, alr1.1, alr2.1
}\if{html}{\out{</div>}}

The group count is \code{length(blueprint$order)}, which is 1 unless the term
was given a \code{by}. The parameter list is \code{\link[=term_params]{term_params()}}, the same before
and after a build, \code{k} alone determining it.

Note that a built structural term is never described as "built": the count
of groups is the tell. \code{\link[=term_is_built]{term_is_built()}} tests for a design block, which
this branch does not have.
}
\examples{
set.seed(1)
dd <- data.frame(t = 1:40, id = rep(1:2, each = 20),
                 y = c(rnorm(20), rnorm(20, 3)))

# A specification, and the same term built over two groups.
regime(2)
term_build(regime(2, by = id, time = t), dd)

# Three regimes carry three levels and six log-ratios.
regime(3)

}
\seealso{
\code{\link[=regime]{regime()}}, \code{\link[=term_params]{term_params()}}.
}
\keyword{internal}
